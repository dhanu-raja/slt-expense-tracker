# ==========================================
# Author: [Your Name]
# Project: SLT Expense Tracker Frontend
# Description: Multi-stage build for React App
# ==========================================

FROM node:20-alpine AS build
LABEL maintainer="[Your Name] <[Your Email]>"

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
# Add custom nginx config for React Router fallback
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
